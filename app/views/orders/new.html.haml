%h2
  = t '.order_lunch'

- form_for @order, orders_path do |f|
  %table
    %thead
      %tr
        %td
        %td
          %b
            = Lunch.human_attribute_name(:vendor_id)
        %td
          %b
            = t '.id'
        %td
          %b
            = Lunch.human_attribute_name(:description)
        %td
          %b
            = Lunch.human_attribute_name(:price)
    %tbody#lunches_list
      - Lunch.all.each do |lunch|
        = render :partial => 'lunches/lunch_to_order', :object => lunch
    %tfoot
      %tr
        %td#submit_buttons
          = f.submit t('order_button')
